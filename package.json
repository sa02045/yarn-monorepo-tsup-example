{
  "packageManager": "yarn@4.2.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all run build"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.3"
  }
}
